"use strict";
var core_1 = require('@angular/core');
function createProviderFactory(name, token, multi) {
    if (token === void 0) { token = new core_1.OpaqueToken(name); }
    if (multi === void 0) { multi = false; }
    return function (factory, deps) {
        if (deps === void 0) { deps = []; }
        return core_1.provide(token, {
            deps: deps,
            multi: multi,
            useFactory: factory
        });
    };
}
exports.createProviderFactory = createProviderFactory;
function compose() {
    var funcs = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        funcs[_i - 0] = arguments[_i];
    }
    return function () {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            args[_i - 0] = arguments[_i];
        }
        if (funcs.length === 0) {
            return args[0];
        }
        var last = funcs[funcs.length - 1];
        var rest = funcs.slice(0, -1);
        return rest.reduceRight(function (composed, f) { return f(composed); }, last.apply(void 0, args));
    };
}
exports.compose = compose;
;
//# sourceMappingURL=util.js.map